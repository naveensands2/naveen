#include <stdio.h>
#include <conio.h>
main()
{
    int i;

    printf("Enter a number: ");
    scanf("%d", &i);
    if(number % 2 == 0)
    printf("%d is even.", number);
    else
        printf("%d is odd.", number);

    return ;
}
